const Order = require('./Order');

// Define any model relationships here if needed in the future
// For example, if you add a User model:
// User.hasMany(Order);
// Order.belongsTo(User);

module.exports = {
  Order
};
